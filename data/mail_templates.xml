<odoo>
  <record id="mail_template_ticket_ack" model="mail.template">
    <field name="name">Nova Support: Ticket Acknowledgement</field>
    <field name="model_id" ref="helpdesk.model_helpdesk_ticket"/>
    <field name="email_from">${(object.team_id and object.team_id.alias_id and object.team_id.alias_id.display_name) or user.email_formatted}</field>
    <field name="email_to">${object.email or object.partner_id.email}</field>
    <field name="subject">[Ticket ${object.id}] ${object.name}</field>
    <field name="body_html" type="html">
      <![CDATA[
      <p>Hello ${object.partner_id.name or 'there'},</p>
      <p>Thanks for contacting <b>Nova Tile and Stone</b> support. Your ticket <b>#${object.id}</b> was created.</p>
      <p><b>Subject:</b> ${object.name}<br/>
         <b>Company:</b> ${object.company_name or ''}<br/>
         <b>Phone:</b> ${object.phone or ''}</p>
      <p>We'll be in touch shortly.</p>
      ]]>
    </field>
  </record>
</odoo>
